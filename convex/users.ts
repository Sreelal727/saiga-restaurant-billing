// File intentionally left as a stub — the previous Convex Auth helpers were
// removed in favor of the simpler PIN-based auth in `convex/auth.ts`.
// Keeping the file (even empty) avoids any cached references in tooling.
export {};
